name: Project Standard CI

on:
  pull_request:
  push:
    branches: [main, master]

concurrency:
  group: project-standard-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  standard:
    uses: franktorok3/Project-Setup/.github/workflows/next-ci.yml@main
